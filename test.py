# import os
# import torch
# from mytransformers import pp
# from mytransformers import utils

if __name__ == "__main__":
    print(5 in [])
    # rank = int(os.environ["RANK"])
    # group = utils.init_distributed()